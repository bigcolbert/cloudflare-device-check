# Cloudflare Device Check V4.4.1 Pages Ready

这是修正后的 Cloudflare Pages GitHub 部署版本。

特点:
- 根目录 Vite 项目
- 自动识别 Vue
- Pages Functions API
- 无需 wrangler deploy

Cloudflare Pages设置:

Framework:
Vue

Build command:
npm run build

Output:
dist

不要填写:
npx wrangler deploy

目录:

index.html
package.json
src/
functions/
worker/
